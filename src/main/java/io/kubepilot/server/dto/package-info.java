/**
 * Wire-format request and response records for the REST layer.
 *
 * <p>Kept separate from {@code common} so the public API can stay stable while internal types
 * evolve independently.
 */
package io.kubepilot.server.dto;
